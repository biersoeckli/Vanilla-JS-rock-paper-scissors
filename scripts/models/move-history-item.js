export default class MoveHistoryItem {
    constructor(points, playerHand, systemHand, playerName) {
        this.points = points;
        this.playerHand = playerHand;
        this.systemHand = systemHand;
        this.playerName = playerName;
    }
}
